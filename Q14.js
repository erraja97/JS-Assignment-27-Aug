//Question 14: Slope is m = (y2-y1)/(x2-x1). Find the slope between point (2, 2) and point(6,10)

let input_X = [2,2];
let input_Y = [6,10];

let slope = (input_Y[1] - input_Y[0]) / (input_X[1] - input_X[0]);
console.log(`Slope is: `, slope);


